package com.purstech.reelfit.exportplugin;

/**
 * Deprecated. Text overlays are now rendered directly inside BlurPadEffect
 * using Android Canvas, so no media3 overlay classes are needed. This stub is
 * intentionally kept (with no imports) so that re-uploading the plugin folder
 * overwrites and neutralises the previous media3-based TextFx implementation.
 * Nothing references this class; it can be deleted at any time.
 */
final class TextFx {
    private TextFx() { }
}
